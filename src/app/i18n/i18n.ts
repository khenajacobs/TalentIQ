// Ideally, I'd prefer to create some configuration to determine the language the application has to run in. 
// In a first phase I'd like to define the language from a property defined in the code base
// This feature would be even more complete if it can come from a user property saved in a database that determines in what language the UI should be visible


// Initialize i18next